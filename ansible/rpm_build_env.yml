---

- name: S3 rpm build setup (Centos 75) (Assumes motr build env is set up)
# Motr build env should be setup and motr and lustre rpms should be installed.
# Clone motr and build using ./autogen.sh && ./configure && make rpms-notests
  hosts: s3devvm
  remote_user: root
  gather_facts: yes
  vars:
    python_version: "{{ 'python36' if ((ansible_distribution == 'CentOS' or ansible_distribution == 'RedHat') and (ansible_distribution_major_version == '7')) else 'python3'}}"

  tasks:
    - import_tasks: common_build_env.yml

    - name: Add motr repository
      yum_repository:
        name: releases_hermi_motr
        description: Yum repo for motr rpms
        baseurl: http://jenkins.mero.colo.seagate.com/share/bigstorage/releases/hermi/last_successful/mero/repo/
        gpgcheck: no
        priority: 1

    - name: Add halon repository
      yum_repository:
        name: releases_hermi_halon
        description: Yum repo for halon rpms
        baseurl: http://jenkins.mero.colo.seagate.com/share/bigstorage/releases/hermi/last_successful/halon/repo/
        gpgcheck: no
        priority: 1
